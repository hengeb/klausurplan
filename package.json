{
  "name": "klausurplan-frontend-tests",
  "private": true,
  "description": "Nur für die Frontend-Tests (jsdom). Wird nicht auf den Server hochgeladen; das Frontend selbst braucht kein Node.",
  "scripts": {
    "test": "node --test tests/js/"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
